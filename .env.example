# Email Configuration (used for Vercel Functions)
# These are already configured in the code, but keep secure

# Gmail Settings (for nodemailer)
# Email: sumanme10@gmail.com
# App Password: sykn fpnn soyc jrcq

# Local Development
# The app runs on: http://localhost:8080
# API calls go to: /api/register
# Vite proxies to: http://localhost:5000 (if Express server is running)

# Production (Vercel)
# API calls go to: /api/register
# Vercel routes to: api/register.js serverless function
# No separate backend needed!
